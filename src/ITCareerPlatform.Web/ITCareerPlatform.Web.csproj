<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ITCareerPlatform</RootNamespace>
    <AssemblyName>ITCareerPlatform.Web</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- EF Core + SQL Server (đúng công nghệ đã chốt) -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.0" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <!-- Băm mật khẩu -->
    <PackageReference Include="BCrypt.Net-Next" Version="4.0.3" />
    <!-- Đọc chữ trong PDF: nội dung trang PDF nằm trong stream nén FlateDecode,
         không thể lấy ra bằng cách lọc byte in được. -->
    <PackageReference Include="PdfPig" Version="0.1.16" />
  </ItemGroup>

</Project>
